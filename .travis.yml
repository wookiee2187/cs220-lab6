
language: c

install: true 

branches:
   only:
    - master
jobs:
  include:
    stage: build
    script:
     - make -C lab6/hello/ clean all 
    artifacts:
      expire_in: 1 hour
      paths:
        - lab6/hello/hello
    stage: test
    script:
      - make -C lab6/hello clean tests
    only:
      - master
    dependencies:
      - hello:build
